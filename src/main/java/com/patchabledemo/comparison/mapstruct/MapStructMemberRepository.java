package com.patchabledemo.comparison.mapstruct;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MapStructMemberRepository extends JpaRepository<MapStructMember, Long> {
}
