package com.patchabledemo.comparison.jsonpatch;

import org.springframework.data.jpa.repository.JpaRepository;

public interface JsonPatchMemberRepository extends JpaRepository<JsonPatchMember, Long> {
}
